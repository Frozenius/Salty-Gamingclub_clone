import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.salty.gamingclub.app',
  appName: 'sgc-page',
  webDir: 'dist/SGC-PAGE',
  bundledWebRuntime: false,
};

export default config;
